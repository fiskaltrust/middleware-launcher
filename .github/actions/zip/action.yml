name: 'zip'
inputs:
  path:
    required: true
    type: string
  vmImage:
    required: true
    type: string
  zipFileName:
    required: true
    type: string
  artifactName:
    required: true
    type: string
    default: "package"

runs:
  using: "composite"
  steps:
    - name: Publish zip packages
      shell: pwsh
      run: |       
        $destinationPath = "./publish/${{ inputs.artifactName }}/"       
        New-Item -ItemType Directory -Force -Path $destinationPath
        $zipFileName = "${{ inputs.zipFileName }}"
        $destinationFile = "${destinationPath}/${zipFileName}.zip"

        $vmImage = "${{ inputs.vmImage }}"
        if($vmImage.StartsWith("windows")) {
          Compress-Archive -Path ${{ inputs.path }}* -DestinationPath ./publish/${{ inputs.artifactName }}/${{ inputs.zipFileName }}.zip
        } else {
          bash -c "cd ${{ inputs.path }} && zip -r $destinationFile ."
        }

        $hash = Get-FileHash ./publish/${{ inputs.artifactName }}/${{ inputs.zipFileName }}.zip -Algorithm SHA256
        $hashbytes = $hash.Hash -split '([A-F0-9]{2})' | foreach-object { if ($_) {[System.Convert]::ToByte($_,16)}}
        $hashstring = [System.Convert]::ToBase64String($hashbytes)
        $hashstring | Set-Content./publish/${{ inputs.artifactName }}/${{ inputs.zipFileName }}.zip.hash
        ls $destinationPath

    - name: Upload Package
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.artifactName }}
        path: $destinationPath